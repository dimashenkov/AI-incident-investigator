{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-sre-mvp/schemas/common.schema.json",
  "title": "Shared definitions",
  "description": "Values that more than one schema needs, in one place.\\n\\nCodex, chunk 0 round 6: these lived inside incident.schema.json and were referenced from the other files by absolute id. Every ref resolved in production, because the validator registers all schemas together — but it made two formerly independent files silently depend on the incident schema, and an absolute $ref is only a name: nothing fetches the file it points at.\\n\\nThe schemas are a package, not four separate documents. Nothing here compiles alone, and a test checks that every cross-file reference points at a schema the validator actually registers.",
  "$defs": {
    "evidenceSource": {
      "description": "Where a fact came from. One carrier: this list also governs what a chat answer may cite, and the two drifting apart would let a message cite a source no evidence can have.",
      "type": "string",
      "enum": [
        "kubernetes",
        "logs",
        "metrics",
        "datadog"
      ]
    },
    "severity": {
      "description": "One carrier for how bad something is, shared by an incoming alert and by an agent's finding.",
      "type": "string",
      "enum": [
        "info",
        "warning",
        "critical"
      ]
    },
    "causeCode": {
      "type": "string",
      "enum": [
        "CONTAINER_OOM",
        "APPLICATION_STARTUP_FAILURE",
        "IMAGE_PULL_FAILURE",
        "READINESS_PROBE_FAILURE",
        "DEPLOYMENT_REGRESSION",
        "CPU_THROTTLING"
      ],
      "description": "The named causes this system can identify. One carrier, referenced by both the incident's root_cause_code and an agent's hypothesis code. Codex, 2026-09-05: keeping them as two enums that a test compares means the lists can still be written apart and only caught afterwards - and one such drift had already made a scenario unsolvable, with CPU_THROTTLING recordable while no agent could propose it. INSUFFICIENT_EVIDENCE is deliberately NOT here: it is a verdict about a whole incident, not a cause an agent can hold a hypothesis about."
    }
  }
}
