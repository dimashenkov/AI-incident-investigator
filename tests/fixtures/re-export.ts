/**
 * A re-export, which transpiles to a form that is not valid in a script.
 *
 * It exists so the generator's refusal has something to refuse.
 */
export { readPrompt } from "../../src/agents/context.js";
