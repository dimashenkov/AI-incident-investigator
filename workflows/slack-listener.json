{
  "name": "AI SRE — Slack listener",
  "nodes": [
    {
      "id": "wh",
      "name": "Slack Events",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        0
      ],
      "webhookId": "ea80d73c-8d02-42ce-bb33-2efe21978bd2",
      "parameters": {
        "httpMethod": "POST",
        "path": "slack-events",
        "responseMode": "responseNode"
      }
    },
    {
      "id": "handle",
      "name": "Handle",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        240,
        0
      ],
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForAllItems",
        "jsCode": "/**\n * The two-way bot's receiving-and-answering logic, kept pure so it can be tested\n * and mutation-covered. The listener workflow (generate-listener.mjs) transpiles\n * this file into its Code nodes, exactly as the incident workflow carries\n * thread.ts — so no imports here, and everything is plain data in, plain data out.\n *\n * The shape of the round trip:\n *   Slack event  -> classifyEvent   -> {challenge}         (verification)\n *                                    -> {event, ignored…}  (a real message)\n *   a real, non-ignored, threaded question\n *                -> shouldReply      -> yes/no\n *   conversations.replies(thread)    -> reportTextFrom     -> the report to answer from\n *   report + question                -> replyMessages      -> the model call (SPENDS)\n *   model response                   -> answerFrom         -> the text to post back\n *\n * Nothing here calls a model or the network; it only decides and formats.\n */\n/** The bot's own Slack user id. A message from it, or carrying a bot_id, or with\n *  any subtype (edit/delete/join), is never answered — that is the anti-loop. */\nconst BOT_USER = \"U0C1ELPQCGH\";\n/**\n * Turn a Slack webhook body into a decision. The url_verification handshake must\n * echo the challenge or Slack never enables events; everything else is an event\n * we classify. A message is IGNORED when anything says a bot or an edit wrote it:\n * a present bot_id, the bot's own user id, or any subtype (Grok, 2026-09-12:\n * subtype alone is unreliable, so bot_id and the user id are the load-bearing\n * checks, and a present subtype is dropped too).\n */\nfunction classifyEvent(body) {\n    const b = (body ?? {});\n    if (b[\"type\"] === \"url_verification\") {\n        return { kind: \"challenge\", challenge: typeof b[\"challenge\"] === \"string\" ? b[\"challenge\"] : \"\" };\n    }\n    const ev = (b[\"event\"] ?? {});\n    const fromBotOrEdit = Boolean(ev[\"bot_id\"]) || ev[\"user\"] === BOT_USER || Boolean(ev[\"subtype\"]);\n    return {\n        kind: \"event\",\n        ignored: fromBotOrEdit,\n        thread_ts: typeof ev[\"thread_ts\"] === \"string\" ? ev[\"thread_ts\"] : null,\n        channel: typeof ev[\"channel\"] === \"string\" ? ev[\"channel\"] : null,\n        user: typeof ev[\"user\"] === \"string\" ? ev[\"user\"] : null,\n        text: fromBotOrEdit ? null : (typeof ev[\"text\"] === \"string\" ? ev[\"text\"] : null),\n        event_id: typeof b[\"event_id\"] === \"string\" ? b[\"event_id\"] : null,\n    };\n}\n/**\n * Whether this classified event should draw a reply. Four conditions, and each\n * closes a way to answer nothing useful or to loop:\n *  - it is an event, not the challenge;\n *  - it was not ignored (not the bot, not an edit);\n *  - it is INSIDE a thread (thread_ts present) — a top-level channel message is\n *    not tied to any incident, so there is nothing to answer from;\n *  - it carries non-empty text and a channel to answer in.\n */\nfunction shouldReply(c) {\n    if (c.kind !== \"event\")\n        return false;\n    if (c.ignored)\n        return false;\n    if (typeof c.thread_ts !== \"string\" || c.thread_ts === \"\")\n        return false;\n    if (typeof c.channel !== \"string\" || c.channel === \"\")\n        return false;\n    if (typeof c.text !== \"string\" || c.text.trim() === \"\")\n        return false;\n    return true;\n}\n/**\n * The incident report to answer from, pulled out of a conversations.replies\n * response. The thread parent (messages[0]) is the bot's original report; its\n * `text` is the plain-thread fallback the report was posted with. Returns null\n * when the call did not succeed or carried no parent — the caller must not\n * invent context, so a missing report means no answer rather than a guess.\n */\nfunction reportTextFrom(repliesBody) {\n    const b = (repliesBody ?? {});\n    if (b[\"ok\"] !== true)\n        return null;\n    const messages = b[\"messages\"];\n    if (!Array.isArray(messages) || messages.length === 0)\n        return null;\n    const parent = (messages[0] ?? {});\n    const text = parent[\"text\"];\n    if (typeof text !== \"string\" || text.trim() === \"\")\n        return null;\n    return text;\n}\n/**\n * The system+user messages for the model. Two things the owner asked for on\n * 2026-09-12, after seeing the bot only ever paraphrase the report:\n *\n *  1. It may REASON and SUGGEST remediation — not just restate the report. But\n *     the suggestions are ADVICE for a human: this system is read-only and takes\n *     no actions, and the prompt says so, so the bot cannot imply it did anything.\n *  2. It answers from the raw observation DATA when given, not only the summary\n *     report — so a question about an exact metric or log line can be answered.\n *\n * The grounding guard stays: every claim must rest on the report or the data; if\n * neither contains the answer, say so rather than invent. `data` is a JSON string\n * of the incident's observations, or \"\" when none was found — in which case only\n * the report is given, and the bot is told the raw data was not available.\n */\nfunction replyMessages(reportText, question, data = \"\") {\n    const system = \"You are the incident-investigation assistant answering a question in a Slack \" +\n        \"thread about ONE incident. You are given the incident report, and sometimes \" +\n        \"the incident's raw observation data. Answer the specific question concisely. \" +\n        \"You MAY reason about the facts and SUGGEST remediation steps — but frame them \" +\n        \"as advice for a human operator: this system is read-only, it takes no actions \" +\n        \"and cannot run anything. Ground every claim in the report or the data; if \" +\n        \"they do not contain what is needed, say so plainly rather than guessing. Do \" +\n        \"not restate the whole report, and answer in prose — do not paste the raw data \" +\n        \"or large JSON blobs into the thread.\";\n    const hasData = typeof data === \"string\" && data.trim() !== \"\";\n    const dataSection = hasData\n        ? `\\n\\nIncident data (raw observations):\\n${data}`\n        : \"\\n\\n(The raw observation data was not available; answer from the report.)\";\n    const user = `Incident report:\\n${reportText}${dataSection}\\n\\nQuestion: ${question}`;\n    return [\n        { role: \"system\", content: system },\n        { role: \"user\", content: user },\n    ];\n}\n/**\n * The answer text out of an OpenAI chat.completions response. Returns null when\n * the response has no content — a null answer must not be posted as if the bot\n * had something to say.\n */\nfunction answerFrom(openaiBody) {\n    const b = (openaiBody ?? {});\n    const choices = b[\"choices\"];\n    if (!Array.isArray(choices) || choices.length === 0)\n        return null;\n    const msg = (choices[0] ?? {})[\"message\"];\n    const content = (msg ?? {})[\"content\"];\n    if (typeof content !== \"string\" || content.trim() === \"\")\n        return null;\n    return content.trim();\n}\n\nconst body = ($input.first() && $input.first().json && $input.first().json.body) || {};\nconst c = classifyEvent(body);\nif (c.kind === \"challenge\") return [{ json: c }];\nreturn [{ json: Object.assign({}, c, { reply: shouldReply(c) }) }];"
      }
    },
    {
      "id": "is-chal",
      "name": "Is challenge",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        480,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "typeValidation": "strict",
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "leftValue": "={{ $json.kind }}",
              "rightValue": "challenge",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ]
        }
      }
    },
    {
      "id": "resp-chal",
      "name": "Respond challenge",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        720,
        -160
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ challenge: $json.challenge }) }}"
      }
    },
    {
      "id": "resp-ack",
      "name": "Respond ack",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        720,
        0
      ],
      "parameters": {
        "respondWith": "text",
        "responseBody": "ok",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "should",
      "name": "Should reply",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        960,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "typeValidation": "strict",
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "leftValue": "={{ $json.reply }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ]
        }
      }
    },
    {
      "id": "seen",
      "name": "Seen event",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1200,
        0
      ],
      "parameters": {
        "resource": "row",
        "operation": "rowNotExists",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "uBZvrUFgQcderwqF"
        },
        "filters": {
          "conditions": [
            {
              "keyName": "event_id",
              "condition": "eq",
              "keyValue": "={{ $json.event_id }}"
            }
          ]
        }
      }
    },
    {
      "id": "mark",
      "name": "Mark seen",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1440,
        0
      ],
      "parameters": {
        "resource": "row",
        "operation": "insert",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "uBZvrUFgQcderwqF"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "event_id": "={{ $json.event_id }}"
          },
          "matchingColumns": [],
          "schema": [
            {
              "id": "event_id",
              "displayName": "event_id",
              "type": "string",
              "canBeUsedToMatch": true,
              "required": false,
              "display": true,
              "defaultMatch": false
            }
          ]
        }
      }
    },
    {
      "id": "fetch",
      "name": "Fetch thread",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1680,
        0
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "eOVr6fQ0yzz3yiwO",
          "name": "Header Auth account"
        }
      },
      "parameters": {
        "method": "GET",
        "url": "https://slack.com/api/conversations.replies",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "channel",
              "value": "={{ $('Handle').first().json.channel }}"
            },
            {
              "name": "ts",
              "value": "={{ $('Handle').first().json.thread_ts }}"
            },
            {
              "name": "limit",
              "value": "1"
            }
          ]
        },
        "options": {}
      }
    },
    {
      "id": "extract-id",
      "name": "Extract id",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1920,
        0
      ],
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForAllItems",
        "jsCode": "/**\n * The two-way bot's receiving-and-answering logic, kept pure so it can be tested\n * and mutation-covered. The listener workflow (generate-listener.mjs) transpiles\n * this file into its Code nodes, exactly as the incident workflow carries\n * thread.ts — so no imports here, and everything is plain data in, plain data out.\n *\n * The shape of the round trip:\n *   Slack event  -> classifyEvent   -> {challenge}         (verification)\n *                                    -> {event, ignored…}  (a real message)\n *   a real, non-ignored, threaded question\n *                -> shouldReply      -> yes/no\n *   conversations.replies(thread)    -> reportTextFrom     -> the report to answer from\n *   report + question                -> replyMessages      -> the model call (SPENDS)\n *   model response                   -> answerFrom         -> the text to post back\n *\n * Nothing here calls a model or the network; it only decides and formats.\n */\n/** The bot's own Slack user id. A message from it, or carrying a bot_id, or with\n *  any subtype (edit/delete/join), is never answered — that is the anti-loop. */\nconst BOT_USER = \"U0C1ELPQCGH\";\n/**\n * Turn a Slack webhook body into a decision. The url_verification handshake must\n * echo the challenge or Slack never enables events; everything else is an event\n * we classify. A message is IGNORED when anything says a bot or an edit wrote it:\n * a present bot_id, the bot's own user id, or any subtype (Grok, 2026-09-12:\n * subtype alone is unreliable, so bot_id and the user id are the load-bearing\n * checks, and a present subtype is dropped too).\n */\nfunction classifyEvent(body) {\n    const b = (body ?? {});\n    if (b[\"type\"] === \"url_verification\") {\n        return { kind: \"challenge\", challenge: typeof b[\"challenge\"] === \"string\" ? b[\"challenge\"] : \"\" };\n    }\n    const ev = (b[\"event\"] ?? {});\n    const fromBotOrEdit = Boolean(ev[\"bot_id\"]) || ev[\"user\"] === BOT_USER || Boolean(ev[\"subtype\"]);\n    return {\n        kind: \"event\",\n        ignored: fromBotOrEdit,\n        thread_ts: typeof ev[\"thread_ts\"] === \"string\" ? ev[\"thread_ts\"] : null,\n        channel: typeof ev[\"channel\"] === \"string\" ? ev[\"channel\"] : null,\n        user: typeof ev[\"user\"] === \"string\" ? ev[\"user\"] : null,\n        text: fromBotOrEdit ? null : (typeof ev[\"text\"] === \"string\" ? ev[\"text\"] : null),\n        event_id: typeof b[\"event_id\"] === \"string\" ? b[\"event_id\"] : null,\n    };\n}\n/**\n * Whether this classified event should draw a reply. Four conditions, and each\n * closes a way to answer nothing useful or to loop:\n *  - it is an event, not the challenge;\n *  - it was not ignored (not the bot, not an edit);\n *  - it is INSIDE a thread (thread_ts present) — a top-level channel message is\n *    not tied to any incident, so there is nothing to answer from;\n *  - it carries non-empty text and a channel to answer in.\n */\nfunction shouldReply(c) {\n    if (c.kind !== \"event\")\n        return false;\n    if (c.ignored)\n        return false;\n    if (typeof c.thread_ts !== \"string\" || c.thread_ts === \"\")\n        return false;\n    if (typeof c.channel !== \"string\" || c.channel === \"\")\n        return false;\n    if (typeof c.text !== \"string\" || c.text.trim() === \"\")\n        return false;\n    return true;\n}\n/**\n * The incident report to answer from, pulled out of a conversations.replies\n * response. The thread parent (messages[0]) is the bot's original report; its\n * `text` is the plain-thread fallback the report was posted with. Returns null\n * when the call did not succeed or carried no parent — the caller must not\n * invent context, so a missing report means no answer rather than a guess.\n */\nfunction reportTextFrom(repliesBody) {\n    const b = (repliesBody ?? {});\n    if (b[\"ok\"] !== true)\n        return null;\n    const messages = b[\"messages\"];\n    if (!Array.isArray(messages) || messages.length === 0)\n        return null;\n    const parent = (messages[0] ?? {});\n    const text = parent[\"text\"];\n    if (typeof text !== \"string\" || text.trim() === \"\")\n        return null;\n    return text;\n}\n/**\n * The system+user messages for the model. Two things the owner asked for on\n * 2026-09-12, after seeing the bot only ever paraphrase the report:\n *\n *  1. It may REASON and SUGGEST remediation — not just restate the report. But\n *     the suggestions are ADVICE for a human: this system is read-only and takes\n *     no actions, and the prompt says so, so the bot cannot imply it did anything.\n *  2. It answers from the raw observation DATA when given, not only the summary\n *     report — so a question about an exact metric or log line can be answered.\n *\n * The grounding guard stays: every claim must rest on the report or the data; if\n * neither contains the answer, say so rather than invent. `data` is a JSON string\n * of the incident's observations, or \"\" when none was found — in which case only\n * the report is given, and the bot is told the raw data was not available.\n */\nfunction replyMessages(reportText, question, data = \"\") {\n    const system = \"You are the incident-investigation assistant answering a question in a Slack \" +\n        \"thread about ONE incident. You are given the incident report, and sometimes \" +\n        \"the incident's raw observation data. Answer the specific question concisely. \" +\n        \"You MAY reason about the facts and SUGGEST remediation steps — but frame them \" +\n        \"as advice for a human operator: this system is read-only, it takes no actions \" +\n        \"and cannot run anything. Ground every claim in the report or the data; if \" +\n        \"they do not contain what is needed, say so plainly rather than guessing. Do \" +\n        \"not restate the whole report, and answer in prose — do not paste the raw data \" +\n        \"or large JSON blobs into the thread.\";\n    const hasData = typeof data === \"string\" && data.trim() !== \"\";\n    const dataSection = hasData\n        ? `\\n\\nIncident data (raw observations):\\n${data}`\n        : \"\\n\\n(The raw observation data was not available; answer from the report.)\";\n    const user = `Incident report:\\n${reportText}${dataSection}\\n\\nQuestion: ${question}`;\n    return [\n        { role: \"system\", content: system },\n        { role: \"user\", content: user },\n    ];\n}\n/**\n * The answer text out of an OpenAI chat.completions response. Returns null when\n * the response has no content — a null answer must not be posted as if the bot\n * had something to say.\n */\nfunction answerFrom(openaiBody) {\n    const b = (openaiBody ?? {});\n    const choices = b[\"choices\"];\n    if (!Array.isArray(choices) || choices.length === 0)\n        return null;\n    const msg = (choices[0] ?? {})[\"message\"];\n    const content = (msg ?? {})[\"content\"];\n    if (typeof content !== \"string\" || content.trim() === \"\")\n        return null;\n    return content.trim();\n}\n\nconst report = reportTextFrom(($('Fetch thread').first() && $('Fetch thread').first().json) || {});\nconst m = report ? report.match(/INC-\\d{4}-\\d{4}/) : null;\nreturn [{ json: { incident_id: m ? m[0] : \"\" } }];"
      }
    },
    {
      "id": "fetch-data",
      "name": "Fetch data",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        2160,
        0
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "resource": "row",
        "operation": "get",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "rKZEwVRRLB3Xb6LR"
        },
        "filters": {
          "conditions": [
            {
              "keyName": "incident_id",
              "condition": "eq",
              "keyValue": "={{ $json.incident_id }}"
            }
          ]
        }
      }
    },
    {
      "id": "build-ask",
      "name": "Build ask",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2400,
        0
      ],
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForAllItems",
        "jsCode": "/**\n * The two-way bot's receiving-and-answering logic, kept pure so it can be tested\n * and mutation-covered. The listener workflow (generate-listener.mjs) transpiles\n * this file into its Code nodes, exactly as the incident workflow carries\n * thread.ts — so no imports here, and everything is plain data in, plain data out.\n *\n * The shape of the round trip:\n *   Slack event  -> classifyEvent   -> {challenge}         (verification)\n *                                    -> {event, ignored…}  (a real message)\n *   a real, non-ignored, threaded question\n *                -> shouldReply      -> yes/no\n *   conversations.replies(thread)    -> reportTextFrom     -> the report to answer from\n *   report + question                -> replyMessages      -> the model call (SPENDS)\n *   model response                   -> answerFrom         -> the text to post back\n *\n * Nothing here calls a model or the network; it only decides and formats.\n */\n/** The bot's own Slack user id. A message from it, or carrying a bot_id, or with\n *  any subtype (edit/delete/join), is never answered — that is the anti-loop. */\nconst BOT_USER = \"U0C1ELPQCGH\";\n/**\n * Turn a Slack webhook body into a decision. The url_verification handshake must\n * echo the challenge or Slack never enables events; everything else is an event\n * we classify. A message is IGNORED when anything says a bot or an edit wrote it:\n * a present bot_id, the bot's own user id, or any subtype (Grok, 2026-09-12:\n * subtype alone is unreliable, so bot_id and the user id are the load-bearing\n * checks, and a present subtype is dropped too).\n */\nfunction classifyEvent(body) {\n    const b = (body ?? {});\n    if (b[\"type\"] === \"url_verification\") {\n        return { kind: \"challenge\", challenge: typeof b[\"challenge\"] === \"string\" ? b[\"challenge\"] : \"\" };\n    }\n    const ev = (b[\"event\"] ?? {});\n    const fromBotOrEdit = Boolean(ev[\"bot_id\"]) || ev[\"user\"] === BOT_USER || Boolean(ev[\"subtype\"]);\n    return {\n        kind: \"event\",\n        ignored: fromBotOrEdit,\n        thread_ts: typeof ev[\"thread_ts\"] === \"string\" ? ev[\"thread_ts\"] : null,\n        channel: typeof ev[\"channel\"] === \"string\" ? ev[\"channel\"] : null,\n        user: typeof ev[\"user\"] === \"string\" ? ev[\"user\"] : null,\n        text: fromBotOrEdit ? null : (typeof ev[\"text\"] === \"string\" ? ev[\"text\"] : null),\n        event_id: typeof b[\"event_id\"] === \"string\" ? b[\"event_id\"] : null,\n    };\n}\n/**\n * Whether this classified event should draw a reply. Four conditions, and each\n * closes a way to answer nothing useful or to loop:\n *  - it is an event, not the challenge;\n *  - it was not ignored (not the bot, not an edit);\n *  - it is INSIDE a thread (thread_ts present) — a top-level channel message is\n *    not tied to any incident, so there is nothing to answer from;\n *  - it carries non-empty text and a channel to answer in.\n */\nfunction shouldReply(c) {\n    if (c.kind !== \"event\")\n        return false;\n    if (c.ignored)\n        return false;\n    if (typeof c.thread_ts !== \"string\" || c.thread_ts === \"\")\n        return false;\n    if (typeof c.channel !== \"string\" || c.channel === \"\")\n        return false;\n    if (typeof c.text !== \"string\" || c.text.trim() === \"\")\n        return false;\n    return true;\n}\n/**\n * The incident report to answer from, pulled out of a conversations.replies\n * response. The thread parent (messages[0]) is the bot's original report; its\n * `text` is the plain-thread fallback the report was posted with. Returns null\n * when the call did not succeed or carried no parent — the caller must not\n * invent context, so a missing report means no answer rather than a guess.\n */\nfunction reportTextFrom(repliesBody) {\n    const b = (repliesBody ?? {});\n    if (b[\"ok\"] !== true)\n        return null;\n    const messages = b[\"messages\"];\n    if (!Array.isArray(messages) || messages.length === 0)\n        return null;\n    const parent = (messages[0] ?? {});\n    const text = parent[\"text\"];\n    if (typeof text !== \"string\" || text.trim() === \"\")\n        return null;\n    return text;\n}\n/**\n * The system+user messages for the model. Two things the owner asked for on\n * 2026-09-12, after seeing the bot only ever paraphrase the report:\n *\n *  1. It may REASON and SUGGEST remediation — not just restate the report. But\n *     the suggestions are ADVICE for a human: this system is read-only and takes\n *     no actions, and the prompt says so, so the bot cannot imply it did anything.\n *  2. It answers from the raw observation DATA when given, not only the summary\n *     report — so a question about an exact metric or log line can be answered.\n *\n * The grounding guard stays: every claim must rest on the report or the data; if\n * neither contains the answer, say so rather than invent. `data` is a JSON string\n * of the incident's observations, or \"\" when none was found — in which case only\n * the report is given, and the bot is told the raw data was not available.\n */\nfunction replyMessages(reportText, question, data = \"\") {\n    const system = \"You are the incident-investigation assistant answering a question in a Slack \" +\n        \"thread about ONE incident. You are given the incident report, and sometimes \" +\n        \"the incident's raw observation data. Answer the specific question concisely. \" +\n        \"You MAY reason about the facts and SUGGEST remediation steps — but frame them \" +\n        \"as advice for a human operator: this system is read-only, it takes no actions \" +\n        \"and cannot run anything. Ground every claim in the report or the data; if \" +\n        \"they do not contain what is needed, say so plainly rather than guessing. Do \" +\n        \"not restate the whole report, and answer in prose — do not paste the raw data \" +\n        \"or large JSON blobs into the thread.\";\n    const hasData = typeof data === \"string\" && data.trim() !== \"\";\n    const dataSection = hasData\n        ? `\\n\\nIncident data (raw observations):\\n${data}`\n        : \"\\n\\n(The raw observation data was not available; answer from the report.)\";\n    const user = `Incident report:\\n${reportText}${dataSection}\\n\\nQuestion: ${question}`;\n    return [\n        { role: \"system\", content: system },\n        { role: \"user\", content: user },\n    ];\n}\n/**\n * The answer text out of an OpenAI chat.completions response. Returns null when\n * the response has no content — a null answer must not be posted as if the bot\n * had something to say.\n */\nfunction answerFrom(openaiBody) {\n    const b = (openaiBody ?? {});\n    const choices = b[\"choices\"];\n    if (!Array.isArray(choices) || choices.length === 0)\n        return null;\n    const msg = (choices[0] ?? {})[\"message\"];\n    const content = (msg ?? {})[\"content\"];\n    if (typeof content !== \"string\" || content.trim() === \"\")\n        return null;\n    return content.trim();\n}\n\nconst h = $('Handle').first().json;\nconst report = reportTextFrom(($('Fetch thread').first() && $('Fetch thread').first().json) || {});\nif (report === null) return [{ json: { ok: false, why: \"no report found in the thread\" } }];\nconst fd = ($('Fetch data').first() && $('Fetch data').first().json) || {};\nconst data = (typeof fd.data === 'string') ? fd.data : \"\";\nreturn [{ json: { ok: true, channel: h.channel, thread_ts: h.thread_ts,\n  messages: replyMessages(report, h.text, data) } }];"
      }
    },
    {
      "id": "has-report",
      "name": "Has report",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2640,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "typeValidation": "strict",
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "leftValue": "={{ $json.ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ]
        }
      }
    },
    {
      "id": "ask",
      "name": "Ask model",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2880,
        0
      ],
      "credentials": {
        "openAiApi": {
          "id": "fcCTZNZiEZhLkGHD",
          "name": "OpenAI account"
        }
      },
      "parameters": {
        "method": "POST",
        "url": "https://api.openai.com/v1/chat/completions",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "openAiApi",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: \"gpt-5\", messages: $json.messages }) }}",
        "options": {}
      }
    },
    {
      "id": "build-reply",
      "name": "Build reply",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3120,
        0
      ],
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForAllItems",
        "jsCode": "/**\n * The two-way bot's receiving-and-answering logic, kept pure so it can be tested\n * and mutation-covered. The listener workflow (generate-listener.mjs) transpiles\n * this file into its Code nodes, exactly as the incident workflow carries\n * thread.ts — so no imports here, and everything is plain data in, plain data out.\n *\n * The shape of the round trip:\n *   Slack event  -> classifyEvent   -> {challenge}         (verification)\n *                                    -> {event, ignored…}  (a real message)\n *   a real, non-ignored, threaded question\n *                -> shouldReply      -> yes/no\n *   conversations.replies(thread)    -> reportTextFrom     -> the report to answer from\n *   report + question                -> replyMessages      -> the model call (SPENDS)\n *   model response                   -> answerFrom         -> the text to post back\n *\n * Nothing here calls a model or the network; it only decides and formats.\n */\n/** The bot's own Slack user id. A message from it, or carrying a bot_id, or with\n *  any subtype (edit/delete/join), is never answered — that is the anti-loop. */\nconst BOT_USER = \"U0C1ELPQCGH\";\n/**\n * Turn a Slack webhook body into a decision. The url_verification handshake must\n * echo the challenge or Slack never enables events; everything else is an event\n * we classify. A message is IGNORED when anything says a bot or an edit wrote it:\n * a present bot_id, the bot's own user id, or any subtype (Grok, 2026-09-12:\n * subtype alone is unreliable, so bot_id and the user id are the load-bearing\n * checks, and a present subtype is dropped too).\n */\nfunction classifyEvent(body) {\n    const b = (body ?? {});\n    if (b[\"type\"] === \"url_verification\") {\n        return { kind: \"challenge\", challenge: typeof b[\"challenge\"] === \"string\" ? b[\"challenge\"] : \"\" };\n    }\n    const ev = (b[\"event\"] ?? {});\n    const fromBotOrEdit = Boolean(ev[\"bot_id\"]) || ev[\"user\"] === BOT_USER || Boolean(ev[\"subtype\"]);\n    return {\n        kind: \"event\",\n        ignored: fromBotOrEdit,\n        thread_ts: typeof ev[\"thread_ts\"] === \"string\" ? ev[\"thread_ts\"] : null,\n        channel: typeof ev[\"channel\"] === \"string\" ? ev[\"channel\"] : null,\n        user: typeof ev[\"user\"] === \"string\" ? ev[\"user\"] : null,\n        text: fromBotOrEdit ? null : (typeof ev[\"text\"] === \"string\" ? ev[\"text\"] : null),\n        event_id: typeof b[\"event_id\"] === \"string\" ? b[\"event_id\"] : null,\n    };\n}\n/**\n * Whether this classified event should draw a reply. Four conditions, and each\n * closes a way to answer nothing useful or to loop:\n *  - it is an event, not the challenge;\n *  - it was not ignored (not the bot, not an edit);\n *  - it is INSIDE a thread (thread_ts present) — a top-level channel message is\n *    not tied to any incident, so there is nothing to answer from;\n *  - it carries non-empty text and a channel to answer in.\n */\nfunction shouldReply(c) {\n    if (c.kind !== \"event\")\n        return false;\n    if (c.ignored)\n        return false;\n    if (typeof c.thread_ts !== \"string\" || c.thread_ts === \"\")\n        return false;\n    if (typeof c.channel !== \"string\" || c.channel === \"\")\n        return false;\n    if (typeof c.text !== \"string\" || c.text.trim() === \"\")\n        return false;\n    return true;\n}\n/**\n * The incident report to answer from, pulled out of a conversations.replies\n * response. The thread parent (messages[0]) is the bot's original report; its\n * `text` is the plain-thread fallback the report was posted with. Returns null\n * when the call did not succeed or carried no parent — the caller must not\n * invent context, so a missing report means no answer rather than a guess.\n */\nfunction reportTextFrom(repliesBody) {\n    const b = (repliesBody ?? {});\n    if (b[\"ok\"] !== true)\n        return null;\n    const messages = b[\"messages\"];\n    if (!Array.isArray(messages) || messages.length === 0)\n        return null;\n    const parent = (messages[0] ?? {});\n    const text = parent[\"text\"];\n    if (typeof text !== \"string\" || text.trim() === \"\")\n        return null;\n    return text;\n}\n/**\n * The system+user messages for the model. Two things the owner asked for on\n * 2026-09-12, after seeing the bot only ever paraphrase the report:\n *\n *  1. It may REASON and SUGGEST remediation — not just restate the report. But\n *     the suggestions are ADVICE for a human: this system is read-only and takes\n *     no actions, and the prompt says so, so the bot cannot imply it did anything.\n *  2. It answers from the raw observation DATA when given, not only the summary\n *     report — so a question about an exact metric or log line can be answered.\n *\n * The grounding guard stays: every claim must rest on the report or the data; if\n * neither contains the answer, say so rather than invent. `data` is a JSON string\n * of the incident's observations, or \"\" when none was found — in which case only\n * the report is given, and the bot is told the raw data was not available.\n */\nfunction replyMessages(reportText, question, data = \"\") {\n    const system = \"You are the incident-investigation assistant answering a question in a Slack \" +\n        \"thread about ONE incident. You are given the incident report, and sometimes \" +\n        \"the incident's raw observation data. Answer the specific question concisely. \" +\n        \"You MAY reason about the facts and SUGGEST remediation steps — but frame them \" +\n        \"as advice for a human operator: this system is read-only, it takes no actions \" +\n        \"and cannot run anything. Ground every claim in the report or the data; if \" +\n        \"they do not contain what is needed, say so plainly rather than guessing. Do \" +\n        \"not restate the whole report, and answer in prose — do not paste the raw data \" +\n        \"or large JSON blobs into the thread.\";\n    const hasData = typeof data === \"string\" && data.trim() !== \"\";\n    const dataSection = hasData\n        ? `\\n\\nIncident data (raw observations):\\n${data}`\n        : \"\\n\\n(The raw observation data was not available; answer from the report.)\";\n    const user = `Incident report:\\n${reportText}${dataSection}\\n\\nQuestion: ${question}`;\n    return [\n        { role: \"system\", content: system },\n        { role: \"user\", content: user },\n    ];\n}\n/**\n * The answer text out of an OpenAI chat.completions response. Returns null when\n * the response has no content — a null answer must not be posted as if the bot\n * had something to say.\n */\nfunction answerFrom(openaiBody) {\n    const b = (openaiBody ?? {});\n    const choices = b[\"choices\"];\n    if (!Array.isArray(choices) || choices.length === 0)\n        return null;\n    const msg = (choices[0] ?? {})[\"message\"];\n    const content = (msg ?? {})[\"content\"];\n    if (typeof content !== \"string\" || content.trim() === \"\")\n        return null;\n    return content.trim();\n}\n\nconst ba = $('Build ask').first().json;\nconst answer = answerFrom(($input.first() && $input.first().json) || {});\nif (answer === null) return [{ json: { ok: false, why: \"the model returned no answer\" } }];\nreturn [{ json: { ok: true, channel: ba.channel, thread_ts: ba.thread_ts, text: answer } }];"
      }
    },
    {
      "id": "has-answer",
      "name": "Has answer",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        3360,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "typeValidation": "strict",
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "leftValue": "={{ $json.ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ]
        }
      }
    },
    {
      "id": "post",
      "name": "Post reply",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3600,
        0
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "eOVr6fQ0yzz3yiwO",
          "name": "Header Auth account"
        }
      },
      "parameters": {
        "method": "POST",
        "url": "https://slack.com/api/chat.postMessage",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json; charset=utf-8"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ channel: $json.channel, thread_ts: $json.thread_ts, text: $json.text }) }}",
        "options": {}
      }
    }
  ],
  "connections": {
    "Slack Events": {
      "main": [
        [
          {
            "node": "Handle",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Handle": {
      "main": [
        [
          {
            "node": "Is challenge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Is challenge": {
      "main": [
        [
          {
            "node": "Respond challenge",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond ack",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Respond ack": {
      "main": [
        [
          {
            "node": "Should reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Should reply": {
      "main": [
        [
          {
            "node": "Seen event",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    },
    "Seen event": {
      "main": [
        [
          {
            "node": "Mark seen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark seen": {
      "main": [
        [
          {
            "node": "Fetch thread",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch thread": {
      "main": [
        [
          {
            "node": "Extract id",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract id": {
      "main": [
        [
          {
            "node": "Fetch data",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch data": {
      "main": [
        [
          {
            "node": "Build ask",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build ask": {
      "main": [
        [
          {
            "node": "Has report",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has report": {
      "main": [
        [
          {
            "node": "Ask model",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    },
    "Ask model": {
      "main": [
        [
          {
            "node": "Build reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build reply": {
      "main": [
        [
          {
            "node": "Has answer",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has answer": {
      "main": [
        [
          {
            "node": "Post reply",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
